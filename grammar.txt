expr     : KEYWORD:VAR IDENTIFIER EX expr
         : (term(PLUS|MINUS) term)*

term     : factor ((MUL|DIV) factor)*

factor   : INT|FLOAT|IDENTIFIER
         : (PLUS|MINUS) factor
         : LPAREN expr RPAREN
